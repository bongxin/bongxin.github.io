---
description: 用户说 commit/push 时先写更新日志再提交推送
alwaysApply: true
---

# Commit / Push 与更新日志

用户说 **commit**、**push**，或 **commit 并 push** 时，助手须按下列顺序执行（不要只提交代码）：

1. **更新日志**：在 `docs/portal/changelog/index.md` **顶部**追加或改写当前版本节  
   - 标题 `## vX.Y.Z`（不要 `【】`）  
   - 含概要 + `🚀 新增` / `🔨 优化` / `🐞 修复`（无则省略空分类）  
   - 归纳**本次相对上次提交的实质变更**，勿流水账堆文件名  
2. **版本号与 Git Tag**（有功能/约定级变更时）：  
   - `package.json` 的 `version` = changelog 最新节 `## vX.Y.Z` 中的 `X.Y.Z`（无 `v` 前缀）  
   - 提交后打 **annotated** tag：`vX.Y.Z`（与 changelog 标题完全一致，含 `v`）  
3. **Commit**：在 **`plume`** 上按仓库 git 安全协议暂存并提交（含 changelog / version；勿提交密钥与无关缓存）  
4. **Push 前**：将 `plume` **合并进 `main`**，在 `main` 上执行 `npm run docs:check:build`，通过后再 push  
5. **Push**：仅当用户明确要求 push（或「commit 并 push」）时执行；推送 `main` 与对应 tag（CI 只听 `main`）

## 注意

- **日常开发只在 `plume`**：改文件 / commit 都在 `plume`；不要把 WIP 直接做在 `main` 上  
- 若发现 `plume` 落后于 `main`，先把 `main` 合回 `plume` 再继续改  
- 未说 push 则只在 `plume` 上 commit（tag 可本地先打，随 push 一起推）  
- changelog 与约定页本身的小改动可并入同一次提交  
- 可选本地钩子：`git config core.hooksPath .githooks`（会在每次 push 自动跑门禁）  
- GitHub Releases / tag 名称必须与更新日志版本节一致，勿另起一套编号  
- 遵守既有 committing / pushing 用户规则（无 amend 除非符合条件等）
