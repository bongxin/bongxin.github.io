---
description: commit/push 固定顺序：先检查，再 plume 写日志提交，合 main，最后 push
alwaysApply: true
---

# Commit / Push 与更新日志

用户说 **commit**、**push**，或 **commit 并 push** 时，助手必须按下列顺序执行（**不得颠倒**；不要先提交再检查、不要在 `main` 上直接改）：

## 固定顺序（硬约束）

1. **检查**：在 **`plume`** 工作区跑通 `npm run docs:check:build`  
   - 未通过 → **禁止**写 changelog / commit / merge / push  
   - 含改样式 / `client.ts` 时：确认无 CSS `@import './…'`（会 postcss ENOENT）；必要时先 `docs:clean-dev` 看一眼首页  
2. **更新日志 + 版本**（在 `plume`）：  
   - `docs/portal/changelog/index.md` **顶部**追加 `## vX.Y.Z`（不要 `【】`）+ 概要；按实际改动写 `🚀 新增` / `🔨 优化` / `🐞 修复`（**有哪类写哪类，不必三项凑齐**）  
   - 功能/约定级变更：同步 `package.json` 的 `version` = `X.Y.Z`  
3. **Commit（仅 `plume`）**：暂存相关文件并提交（含 changelog / version；勿提交密钥与无关缓存）  
4. **Tag（本地）**：`git tag -a vX.Y.Z`（与 changelog 标题一致）  
5. **合并**：`plume` → **`main`**（仅此时改 `main`）  
6. **Push**：仅当用户明确要求 push（或「commit 并 push」）时：推送 `main`、对应 tag；建议同步推 `plume`。CI 只监听 `main`

未说 push → 做到步骤 3（或 4）即可，停在 `plume`。

## 注意

- **日常开发只在 `plume`**：所有 WIP / commit 都在 `plume`；发现落后先把 `main` 合回 `plume`  
- **禁止**：先 commit 再 `docs:check:build`；先 merge/`push` 再检查；在 `main` 上直接改功能  
- changelog 与约定页小改可并入同一次提交  
- 可选：`git config core.hooksPath .githooks`（push 时再跑门禁，**不能代替**步骤 1）  
- GitHub tag 名与更新日志 `## vX.Y.Z` 一致；遵守既有 git 安全协议（无 amend 除非符合条件）
