---
description: 正文不写实现说明/布局套话，只放用户可读内容
alwaysApply: true
---

# 正文忌套话

面向读者的 Markdown **只写内容**，不要写给维护者看的实现说明。

## 禁止出现在正文

- 「左侧侧栏 / 右侧大纲 / 页内导航」类布局提示
- 「使用主题某某组件」「`::: xxx` / LinkCard / Markmap」等实现说明
- 「写法对齐某某站」「完整提交见…」等元叙述（除非本身是正文主题）
- 「如何添加照片 / 如何启用 markdown」类投稿指南（放到 `dev_memory` 或规则）

## 正确做法

- 门户页：frontmatter 后直接 `##` 与正文内容
- 约定、配置、坑点：写 `dev_memory/*.md` 或 `.cursor/rules/`
- 交叉链接可以保留（如「见 [环境说明](/environment/yudao/)」），那是导航不是套话
