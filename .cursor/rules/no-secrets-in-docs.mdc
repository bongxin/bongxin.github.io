---
description: 文档与仓库禁止提交真实数据库/云凭据
alwaysApply: true
---

# 文档禁止真实凭据

本仓库为**公开站点源码**。正文、配置示例、SQL 样例中：

## 禁止

- 真实 JDBC / Redis / Mongo 主机（含自有公网域名端口上的库）
- 真实 `password` / `MYSQL_ROOT_PASSWORD` / token / API key
- 含外网地址与口令的 SQL `INSERT` 数据源记录

## 正确写法

- 主机：`127.0.0.1`、`db.example.com`、`redis.example.com`
- 口令：`<your-password>` 或注释说明「换成自己的」
- 误推后：**先改仓库脱敏**，再**立刻轮换**线上口令；历史 commit 仍可能残留，必要时改密 + 考虑历史清理

与 `/conventions/`「静态资源」节中的凭据约定保持一致。
